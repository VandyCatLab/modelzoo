Ok so the main issue was that you didn't specify the nodes to use, and this stuff is only CPU-reproducible across machines that have the same CPU.
On the next round, keep everything the same except make sure the same nodes are used for all instances.
